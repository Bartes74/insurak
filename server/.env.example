# Server Configuration
PORT=5001
NODE_ENV="development"

# Security
# REPLACE THIS WITH A STRONG RANDOM STRING IN PRODUCTION!
JWT_SECRET="dev-secret-change-me-in-prod"

# Database
# For SQLite (default):
DATABASE_URL="file:./dev.db"

# Rate Limiting (Protection against brute-force)
# Max requests per 15-minute window for sensitive endpoints (login, register)
AUTH_RATE_MAX=100
# Max requests per 15-minute window for file uploads
UPLOAD_RATE_MAX=50

# Email Configuration (Optional - for Password Reset)
# If not provided, reset tokens will be logged to console in dev mode
# SMTP_HOST="smtp.example.com"
# SMTP_PORT=587
# SMTP_USER="user@example.com"
# SMTP_PASS="password"
# FROM_EMAIL="noreply@insurak.pl"
